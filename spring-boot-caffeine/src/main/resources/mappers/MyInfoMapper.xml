<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.caffeine.mapper.MyInfoMapper">

    <insert id="insert">
        insert into my_db.my_info (info_id,user_name)
        values (#{myInfo.infoId},
                #{myInfo.userName})
    </insert>
</mapper>